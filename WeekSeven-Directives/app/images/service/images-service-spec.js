describe('imagesService', function() {

  beforeEach(module('imagesModule'));

  it('should ...', inject(function(imagesService) {

    //expect(imagesService.doSomething()).toEqual('something');

  }));

});
