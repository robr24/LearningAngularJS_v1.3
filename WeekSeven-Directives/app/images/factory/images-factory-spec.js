describe('imagesFactory', function() {

  beforeEach(module('imagesModule'));

  it('should ...', inject(function(imagesFactory) {

    //expect(imagesFactory.doSomething()).toEqual('something');

  }));

});
